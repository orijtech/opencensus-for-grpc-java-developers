syntax = "proto3";

package capitalize;

option java_package = "io.ocgrpc.capitalize";

message Payload {
    int32 id    = 1;
    bytes data  = 2;
}

service Fetch {
    rpc Capitalize(Payload) returns (Payload) {}
}
